import { colors } from './variables'

const styles = {
  block: {
    padding: '10px',
    backgroundColor: colors.primarColor,
    color: 'white',
    position: 'fixed',
    bottom: '0',
    left: '0',
    width: '100%'
  }
}

export default styles